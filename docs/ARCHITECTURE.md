# 📐 Documentación Arquitectónica

## Flight Network Graph API

---

## 1. Introducción

Este documento describe la arquitectura del sistema **Flight Network Graph API**, una aplicación serverless que modela y analiza redes de vuelos utilizando estructuras de grafos.

### 1.1 Propósito
Proporcionar una API REST que permita realizar operaciones de análisis sobre una red de aeropuertos y vuelos, incluyendo búsqueda de rutas óptimas, detección de hubs y análisis de conectividad.

### 1.2 Alcance
- Gestión de grafos de vuelos
- Operaciones de análisis (caminos, clusters, centralidad)
- Despliegue en AWS mediante arquitectura serverless
- CI/CD automatizado

---

## 2. Vista de Contexto

```
                    ┌─────────────────────────────────────────┐
                    │           FLIGHT NETWORK API            │
                    │                                         │
    ┌─────────┐     │  ┌─────────┐  ┌─────────┐  ┌─────────┐ │
    │ Cliente │────▶│  │   API   │  │ Lambda  │  │  Datos  │ │
    │(Postman)│◀────│  │ Gateway │  │Functions│  │  (S3)   │ │
    └─────────┘     │  └─────────┘  └─────────┘  └─────────┘ │
                    │                                         │
                    └─────────────────────────────────────────┘
```

---

## 3. Nivel de Aplicación

### 3.1 Diagrama de Componentes

```
┌────────────────────────────────────────────────────────────────────┐
│                         CAPA DE PRESENTACIÓN                        │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                      API Gateway (REST)                       │  │
│  │  /airports  /shortest-path  /hubs  /clusters  /connections   │  │
│  └──────────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌────────────────────────────────────────────────────────────────────┐
│                         CAPA DE NEGOCIO                            │
│  ┌─────────────────────┐    ┌─────────────────────────────────┐   │
│  │  graph_operations   │    │          FlightGraph            │   │
│  │     (Lambda)        │───▶│         (Modelo)                │   │
│  │                     │    │                                 │   │
│  │ - Routing           │    │ - shortest_path()               │   │
│  │ - Request parsing   │    │ - all_paths()                   │   │
│  │ - Response format   │    │ - get_hubs()                    │   │
│  └─────────────────────┘    │ - get_clusters()                │   │
│                             │ - get_isolated_nodes()          │   │
│                             └─────────────────────────────────┘   │
└────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌────────────────────────────────────────────────────────────────────┐
│                          CAPA DE DATOS                             │
│  ┌─────────────────────┐    ┌─────────────────────────────────┐   │
│  │   airports.json     │    │         flights.json            │   │
│  │                     │    │                                 │   │
│  │ - code              │    │ - origin                        │   │
│  │ - name              │    │ - destination                   │   │
│  │ - city              │    │ - distance                      │   │
│  │ - country           │    │                                 │   │
│  └─────────────────────┘    └─────────────────────────────────┘   │
└────────────────────────────────────────────────────────────────────┘
```

### 3.2 Componentes Principales

| Componente | Responsabilidad | Tecnología |
|------------|-----------------|------------|
| **API Gateway** | Enrutamiento HTTP, autenticación | AWS API Gateway |
| **graph_operations** | Handler Lambda, lógica de routing | Python 3.11 |
| **FlightGraph** | Modelo del grafo, algoritmos | Python + NetworkX |
| **helpers** | Utilidades (parsing, formateo) | Python |
| **Data Layer** | Persistencia de datos | JSON / S3 |

### 3.3 Flujo de Datos

```
┌──────────┐    HTTP     ┌─────────────┐    Invoke    ┌──────────┐
│  Client  │───Request──▶│ API Gateway │─────────────▶│  Lambda  │
└──────────┘             └─────────────┘              └────┬─────┘
                                                          │
     ┌────────────────────────────────────────────────────┘
     │
     ▼
┌──────────────────────────────────────────────────────────────────┐
│                        Lambda Function                            │
│  1. Parse request (path, query params)                           │
│  2. Initialize graph (load airports + flights)                   │
│  3. Execute graph operation (NetworkX)                           │
│  4. Format response (JSON)                                       │
│  5. Return to API Gateway                                        │
└──────────────────────────────────────────────────────────────────┘
     │
     ▼
┌──────────┐   HTTP      ┌─────────────┐
│  Client  │◀──Response──│ API Gateway │
└──────────┘             └─────────────┘
```

### 3.4 Operaciones del Sistema

| Endpoint | Operación | Algoritmo |
|----------|-----------|-----------|
| `/shortest-path` | Camino mínimo | Dijkstra |
| `/all-paths` | Todos los caminos | DFS |
| `/longest-path` | Camino más largo | DFS + max |
| `/hubs` | Nodos más conectados | Degree centrality |
| `/clusters` | Detección de comunidades | Greedy modularity |
| `/isolated` | Nodos aislados | Isolates detection |
| `/connections` | Vecinos de un nodo | Neighbors |
| `/by-degree` | Filtrar por grado | Degree filter |

---

## 4. Nivel de Tecnología

### 4.1 Diagrama de Infraestructura AWS

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              AWS CLOUD                                   │
│                                                                          │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                         VPC (opcional)                          │   │
│   │                                                                 │   │
│   │  ┌─────────────┐     ┌─────────────┐     ┌─────────────┐       │   │
│   │  │    API      │     │   Lambda    │     │     S3      │       │   │
│   │  │  Gateway    │────▶│  Function   │────▶│   Bucket    │       │   │
│   │  │             │     │             │     │             │       │   │
│   │  │ REST API    │     │ Python 3.11 │     │ flight-data │       │   │
│   │  │ HTTPS       │     │ NetworkX    │     │ .json files │       │   │
│   │  └─────────────┘     └──────┬──────┘     └─────────────┘       │   │
│   │                             │                                   │   │
│   │                             ▼                                   │   │
│   │                      ┌─────────────┐                           │   │
│   │                      │ CloudWatch  │                           │   │
│   │                      │   Logs      │                           │   │
│   │                      └─────────────┘                           │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 4.2 Stack Tecnológico

| Capa | Tecnología | Versión | Propósito |
|------|------------|---------|-----------|
| **Runtime** | Python | 3.11 | Lenguaje principal |
| **Grafos** | NetworkX | 3.2.1 | Algoritmos de grafos |
| **AWS SDK** | Boto3 | 1.34.0 | Interacción con AWS |
| **API** | API Gateway | - | Exposición REST |
| **Compute** | Lambda | - | Ejecución serverless |
| **Storage** | S3 | - | Almacenamiento datos |
| **IaC** | SAM | - | Infrastructure as Code |
| **CI/CD** | GitHub Actions | - | Automatización |
| **Local Dev** | LocalStack | latest | Emulación AWS local |
| **Containers** | Docker | - | LocalStack |
| **Testing** | Pytest | 7.4.3 | Tests unitarios |

### 4.3 Diagrama de Despliegue

```
┌─────────────────────────────────────────────────────────────────┐
│                      DESARROLLO LOCAL                            │
│                                                                  │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐       │
│  │   VS Code    │    │   Docker     │    │  LocalStack  │       │
│  │              │    │  Compose     │───▶│              │       │
│  │  - Python    │    │              │    │ - Lambda     │       │
│  │  - Tests     │    │              │    │ - S3         │       │
│  │  - Debug     │    │              │    │ - API GW     │       │
│  └──────────────┘    └──────────────┘    └──────────────┘       │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
           │
           │ git push
           ▼
┌─────────────────────────────────────────────────────────────────┐
│                      GITHUB ACTIONS                              │
│                                                                  │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐       │
│  │   Checkout   │───▶│  Run Tests   │───▶│   SAM Build  │       │
│  └──────────────┘    └──────────────┘    └──────┬───────┘       │
│                                                 │               │
└─────────────────────────────────────────────────│───────────────┘
                                                  │
                                                  │ sam deploy
                                                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                         AWS CLOUD                                │
│                                                                  │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐       │
│  │ CloudFormation│───▶│   Lambda    │    │     S3       │       │
│  │    Stack      │    │  Function   │◀──▶│    Bucket    │       │
│  └──────────────┘    └──────────────┘    └──────────────┘       │
│                             │                                    │
│                             ▼                                    │
│                      ┌──────────────┐                           │
│                      │ API Gateway  │                           │
│                      │   (REST)     │                           │
│                      └──────────────┘                           │
└─────────────────────────────────────────────────────────────────┘
```

### 4.4 Configuración de Recursos

#### Lambda Function
```yaml
Runtime: python3.11
MemorySize: 256 MB
Timeout: 30 seconds
Handler: lambdas.graph_operations.lambda_handler
```

#### API Gateway Endpoints
| Método | Path | Descripción |
|--------|------|-------------|
| GET | /airports | Lista aeropuertos |
| GET | /stats | Estadísticas del grafo |
| GET | /shortest-path | Camino más corto |
| GET | /all-paths | Todos los caminos |
| GET | /hubs | Aeropuertos hub |
| GET | /isolated | Nodos aislados |
| GET | /connections | Conexiones directas |
| GET | /by-degree | Filtrar por grado |
| GET | /clusters | Detectar comunidades |
| GET | /longest-path | Camino más largo |

---

## 5. Decisiones de Diseño

### 5.1 ¿Por qué Serverless?
- **Escalabilidad automática**: Lambda escala según demanda
- **Costo eficiente**: Pago por uso real
- **Sin gestión de servidores**: AWS gestiona la infraestructura
- **Despliegue rápido**: SAM simplifica el deployment

### 5.2 ¿Por qué NetworkX?
- **Biblioteca madura**: Ampliamente usada en ciencia de datos
- **Algoritmos incluidos**: Dijkstra, BFS, DFS, clustering
- **Fácil de usar**: API intuitiva en Python
- **Rendimiento**: Optimizada para grafos medianos

### 5.3 ¿Por qué LocalStack?
- **Desarrollo local**: Sin costos de AWS durante desarrollo
- **Paridad con producción**: Mismos servicios que AWS
- **CI/CD local**: Tests sin conexión a internet
- **Rápido feedback**: No hay latencia de red

---

## 6. Modelo de Datos

### 6.1 Grafo

```
          ┌─────┐
          │ LHR │
          └──┬──┘
    ┌────────┼────────┐
    ▼        ▼        ▼
┌─────┐  ┌─────┐  ┌─────┐
│ CDG │  │ FRA │  │ JFK │
└──┬──┘  └──┬──┘  └──┬──┘
   │        │        │
   ▼        ▼        ▼
┌─────┐  ┌─────┐  ┌─────┐
│ MAD │◀─│ IST │  │ MIA │
└──┬──┘  └─────┘  └──┬──┘
   │                 │
   ▼                 ▼
┌─────┐           ┌─────┐
│ BCN │           │ LAX │
└─────┘           └─────┘
```

### 6.2 Estructura de Nodo (Aeropuerto)
```json
{
  "code": "MAD",
  "name": "Adolfo Suárez Madrid-Barajas",
  "city": "Madrid",
  "country": "Spain"
}
```

### 6.3 Estructura de Arista (Vuelo)
```json
{
  "origin": "MAD",
  "destination": "BCN",
  "distance": 483
}
```

---

## 7. Pipeline CI/CD

```
┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐
│  Push   │───▶│  Test   │───▶│  Build  │───▶│ Deploy  │
│  Code   │    │ (pytest)│    │  (SAM)  │    │  (AWS)  │
└─────────┘    └─────────┘    └─────────┘    └─────────┘
                   │
                   ▼
              ┌─────────┐
              │  Lint   │
              │(flake8) │
              └─────────┘
```

### Stages:
1. **Test**: Ejecuta pytest con 18 tests
2. **Lint**: Verifica estilo de código con flake8
3. **Build**: Empaqueta la aplicación con SAM
4. **Deploy**: Despliega a AWS (solo en main)

---

## 8. Conclusiones

Esta arquitectura proporciona:
- ✅ **Escalabilidad**: Serverless auto-escala
- ✅ **Mantenibilidad**: Código modular y testeable
- ✅ **Costo-eficiencia**: Pago por uso
- ✅ **DevOps**: CI/CD automatizado
- ✅ **Desarrollo local**: LocalStack para testing

---

*Documento generado para el proyecto de Tecnologías de Servicios para Ciencia de Datos - ULPGC*
